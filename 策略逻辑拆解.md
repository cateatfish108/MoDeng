# MoDeng（魔灯）策略逻辑拆解

> 本文档将魔灯量化软件中的所有策略逻辑从代码中拆解出来，按"实时盯盘 → 选股海选 → 大盘预测 → 回测"四大类组织，并标注每条策略对应的源码位置、计算公式与触发条件。

---

## 总览

| 类别 | 策略名称 | 核心思想 | 主要源码 |
|------|----------|----------|----------|
| 实时盯盘 | **动态网格交易** | 用标准差算网格大小，价格突破网格则提示买/卖 | `Function/GUI/GUI_main/reseau_judge_class.py`、`SDK/StdForReseau/Sub.py` |
| 实时盯盘 | **RSV 网格偏置** | 用 RSV（未成熟随机值）把网格在买/卖方向上做非对称分配 | `Function/GUI/GUI_main/cal_rsv_class.py` |
| 实时盯盘 | **波动提示** | 与上次提示价比较，超过网格就闪动任务栏图标 | `reseau_judge_class.py::fluctuate_judge` |
| 选股海选 | **海选规则引擎** | 可配置的多规则按优先级串行过滤股票池 | `Function/SeaSelect/Sub/select_class.py` |
| 选股海选 | **MACD 反转 / SAR 反转 / RSI / 涨跌幅 / 上市年龄 / 价格分位** | 6 类可组合的过滤子规则 | 同上 + `Function/SeaSelect/Sub/Sub.py` |
| 大盘预测 | **LSTM 次日指数预测** | 用 LSTM 预测次日上证/深证/创业板的高低收 | `Function/LSTM/AboutLSTM/Test/TomorrowPredict.py` |
| 辅助 | **M20 均线穿越** | 监控价格穿越 N 日均线的方向与时间 | `Function/M20/m20_class.py` |
| 辅助 | **相对大盘强弱** | 个股变化率减去所属板块变化率 | `SDK/RelativeChangeStrategySub.py` |
| 验证 | **网格回测** | 在历史数据上重放网格逻辑，统计买卖点 | `HuiCe/huice_main.py`、`HuiCe/Sub.py` |

---

## 一、实时盯盘策略

盯盘是软件的核心功能：交易时段每 30 秒遍历持仓/关注的股票，必要时以任务栏图标闪动给出买卖提示。它由三个互相配合的子策略组成。

### 1.1 动态网格大小计算（Reseau）

**源码**：`SDK/StdForReseau/Sub.py::Reseau`

网格交易的"格子大小"不是固定值，而是根据近期价格波动（标准差）动态计算的。

**计算步骤**：

1. 取该股票近 10 个交易日的日 K（不足 7 条则取近 30 天）。
2. 分别用**快窗口 `quick=3`** 和**慢窗口 `slow=6`**，在滑动窗口内对 `[close, low, high]` 三列的所有值求标准差：

   ```
   std_w(idx) = std( df[idx-w+1 : idx][['close','low','high']] )
   ```

3. 网格基准单位取两个窗口标准差的均值：

   ```
   std_m = mean(std_3, std_6)
   earn_threshold_unit = std_m  （取最后一根）
   ```

> 含义：波动越大，格子越大，避免在剧烈波动中频繁触发；波动越小，格子越细。

### 1.2 RSV 网格偏置（非对称网格）

**源码**：`Function/GUI/GUI_main/cal_rsv_class.py::RSV`

普通网格买卖阈值对称，魔灯用 **RSV** 把网格在买、卖方向上做非对称分配——价格处于近期高位时倾向更容易卖、更难买，反之亦然。

**RSV 计算**（`add_rsv`，m=5 日）：

```
low_M5   = MA(low, 5)
high_M5  = MA(high, 5)
close_M5 = MA(close, 5)

RSV = (close_M5 - low_M5) / (high_M5 - low_M5)      # 取值 0~1
# 若 high_M5 == low_M5（近期无波动），RSV 记为 0.5
```

RSV 在每只关注股票上预先计算（数据源 400 日 K 的最后一根），缓存在全局 `RSV_Record`。

**用 RSV 切分网格**（`reseau_judge_class.py::cal_reseau`）：

```
thh_sale = earn_threshold_unit * 2 * RSV          # 卖出网格
thh_buy  = earn_threshold_unit * 2 * (1 - RSV)    # 买入网格
```

- RSV 越大（价格处于近期高位）→ 卖出网格 `thh_sale` 越大、买入网格 `thh_buy` 越小。
- RSV 越小（价格处于近期低位）→ 更容易触发买入提示。

### 1.3 买卖触发判断（bs_judge）

**源码**：`reseau_judge_class.py::bs_judge`

记号：
- `current_price`：实时价（聚宽实时接口 `get_current_price_JQ`）
- `last_p`：上次操作价（`opt_record` 中的 `p_last`）
- `b_p_min`：历史买入操作里的**最低买入价** `min(b_opt.p)`
- `pcr`：用户配置的最小操作幅度（`config['pcr']/100`），过滤掉太小的波动

**卖出条件**（同时满足）：

```
(current_price - b_p_min) > thh_sale              # 相对最低买入价的涨幅超过卖出网格
AND (current_price - b_p_min) / b_p_min >= pcr    # 且涨幅比例不小于最小操作幅度
→ 提示「触发卖出网格！可以考虑卖出！」
```

**买入条件**（同时满足）：

```
(current_price - last_p) < -thh_buy               # 相对上次操作价的跌幅超过买入网格
AND (current_price - last_p) / b_p_min <= -pcr    # 且跌幅比例不小于最小操作幅度
→ 提示「触发买入网格！可以考虑买入！」
```

否则：未触发任何警戒线，静默。

### 1.4 防重复提示机制（has_flashed_flag）

**源码**：`reseau_judge_class.py::bs_note` / `set_has_flashed_flag`

- 一旦触发买/卖提示并闪动，会把该股票的 `has_flashed_flag` 置为 `True` 并写入 `opt_record` json。
- 此后该股**不再重复阈值判断**，直到用户在"灯神"中真正录入一次 bs 操作（买卖记录），标志位才复位。
- 设计目的：避免同一信号反复打扰用户。

### 1.5 波动提示（fluctuate_judge，独立于买卖）

**源码**：`reseau_judge_class.py::fluctuate_judge`

与买卖判断并行的一条轻量提示：以**上次提示价 `last_prompt_point`** 为基准，只要价格偏离超过对应网格就提示一次并刷新基准价。

```
若 last_prompt_point 为空/-1 → 用当前价初始化，返回
若 (current_price - last_prompt_point) > thh_sale → 「上涨幅度超过卖出网格」提示
若 (current_price - last_prompt_point) < -thh_buy → 「下跌幅度超过买入网格」提示
触发后：last_prompt_point = current_price（刷新基准）
```

> 与 `bs_judge` 的区别：`fluctuate_judge` 基准是"上次提示价"，关注的是相对上次提示的纯波动；`bs_judge` 基准是"历史最低买入价/上次操作价"，关注的是真实盈亏意义上的买卖点。

---

## 二、每日海选（选股）策略

**源码**：`Function/SeaSelect/Sub/select_class.py`

海选是一个**可配置的规则引擎**：每只候选股票封装成 `SeaSelect` 对象，规则保存在 json 中，按 `priority` 优先级**串行过滤**整个股票池，最终把通过的股票生成 PDF 报告。

### 2.1 规则引擎执行流程（sea_select）

```
1. 读取 json 中的 filter_rule 列表
2. 按 priority 分组并升序排序
3. 依次执行每条规则 → 每次过滤都缩小 stk_list_ss
4. 全部规则跑完后，对幸存股票生成走势图 + PDF 报告
```

规则分发见 `rule_execute`，支持以下 6 种规则类型。

### 2.2 六大过滤规则

#### ① MACD 反转（macd反转）

**源码**：`macd_stray_judge`

```
MACD = talib.MACD(close, fast=6, slow=12, signal=9)
取最近 3 根 MACD 值，若中间那根是三者最小值 → 判定为底部反转（保留）
```

> 即检测 MACD 的 V 型底（`macd[-2] == min(macd[-3:])`）。可作用于日线/周线/月线/半小时线。

#### ② SAR 反转（sar反转）

**源码**：`sar_stray_judge_sub`

```
SAR = talib.SAR(high, low, acceleration=0.05, maximum=0.2)
比较最近两根的 SAR 与 close 的相对位置：
  上一根 SAR 在价上方、当前根 SAR 跌到价下方 → 返回 1（向上反转，保留）
  反向 → -1（向下反转）
  否则 → 0
海选只保留返回值 == 1 的股票
```

#### ③ RSI 区间（rsi）

**源码**：`judge_rsi_sub`

```
RSI = talib.RSI(close, timeperiod=span)   # span ∈ {5,12,30}
若 threshold_low <= RSI(最后一根) <= threshold_high → 保留
```

#### ④ 上市年龄（上市年龄）

**源码**：`filter_by_age` / `cal_age`

```
age = 当前年份 - 上市年份（来自 tushare timeToMarket）
保留 age_low <= age <= age_high 的股票
（main 测试里写死过滤掉 age <= 4 的次新股）
```

#### ⑤ 当日涨跌幅（当日涨跌幅）

**源码**：`filter_by_today_cp`

```
cp = tushare get_today_all() 的 changepercent
保留 cp_low <= cp <= cp_high 的股票
（文档描述的经典规则：当日涨幅 > 3%）
```

#### ⑥ 价格分位（close_rank）

**源码**：`cal_close_rank` / `filter_by_close_rank`

```
取最近 amount 根 close，做 Min-Max 归一化：
  close_rank = (close - min) / (max - min)  → 取最后一根，0~1
保留 rank_low/100 <= close_rank <= rank_high/100 的股票
```

> 用于筛选"当前价格处于近期低位/高位"的股票。

### 2.3 周线/月线数据的构造

**源码**：`add_week_month_data` / `get_week_month_index_data_sub`

软件不直接下载周月 K，而是从日线**重采样**：

```
规整：df 截取为 floor(len/20)*20 - 19 根
周线：取每周「星期四(day==4)」那根（+ 最后一根），MACD 用 fast=6/slow=12/signal=9
月线：每隔 20 根取一根（::20），MACD 用 fast=4/slow=8/signal=9
```

> 文档中提到的"MACD 月线没有处于顶部"即通过对月线序列应用 `macd_stray_judge` 类逻辑实现。

---

## 三、LSTM 大盘指数预测

**源码**：`Function/LSTM/AboutLSTM/Test/TomorrowPredict.py`、模型类 `SDK/LSTM_Class.py`

用为每个指数/标的单独训练好的 LSTM 模型，预测**次日的 high / low / close**。

### 3.1 特征工程

```
输入数据：tushare 近 (N_STEPS+30) 根日 K
特征列 feature_cols 含：close, high, low, open, volume, rank
新增特征：
  m9      = MA(close, 9)
  diff_m9 = (close - m9) / close                       # 乖离度
  rank    = 100 - relative_rank(历史 diff_m9, 当前 diff_m9)   # 乖离度的历史分位
归一化：
  价格列 (close/high/low/open) 用该股历史 p_min/p_max 做 Min-Max
  成交量用 v_min/v_max 做 Min-Max
  （极值保存在 stk_max_min.json）
```

### 3.2 模型与预测

```
模型：多层 LSTM（HIDDEN_SIZE 隐含层、NUM_LAYERS 层，N_STEPS=20 时间步）
每个「标的_label」组合一个独立 ckpt 模型，如 300508_high
输入：最近 20 根的归一化特征
输出：归一化的预测值 r_rela
反归一化：predict = p_min + (p_max - p_min) * r_rela
```

`predict_tomorrow_index` 循环对 `['sh','sz','cyb']`（上证/深证/创业板）分别预测高低收，并通过 QQ 推送结果。

> ⚠️ 依赖 TensorFlow 1.x（`tf.Session`、`tf.placeholder`、`saver.restore`），为静态图时代写法。

---

## 四、辅助策略

### 4.1 M20 均线穿越监控

**源码**：`Function/M20/m20_class.py`

监控价格相对 N 日均线（默认 M20）的穿越方向与时间：

```
m_N        = MA(close, N)
m_pn_N     = (close - m_N >= 0)            # True=在均线上方
pn_pot_N   = (m_pn_N != m_pn_N.shift(1))   # 状态翻转点=穿越点
```

- `MNote.m_rt_judge`：实时判断当前是否刚穿越均线。
- `get_last_m_stray`：报告"最近一次穿越发生在 X 天 Y 分钟前，为涨破/跌破"。
- `AverageStatics.splice_m_seg`：把价格按穿越点切成线段，统计每段的最大涨幅、最小跌幅、以及该段末尾的 MACD/BIAS 分位——用于研究"穿越均线后通常能涨多少"。

### 4.2 相对大盘强弱

**源码**：`SDK/RelativeChangeStrategySub.py`

衡量个股相对其所属板块的超额表现：

```
个股日变化率   change_ratio   = (close - open) / open
板块日变化率   c_change_ratio = (板块close - 板块open) / 板块open
相对变化差     ratio_diff     = change_ratio - c_change_ratio
```

板块归类规则（按代码前缀）：

```
300xxx → 创业板(cyb)    002xxx → 中小板(zxb)
6xxxxx → 沪市(sh)       0xxxxx → 深市(sz)
```

> `ratio_diff > 0` 表示个股跑赢板块。

---

## 五、网格策略回测

**源码**：`HuiCe/huice_main.py`、`HuiCe/Sub.py`

在历史数据上重放盯盘网格逻辑，验证买卖点的合理性。

**交易成本建模**（`HuiCe/Sub.py::bs_opt` / `cal_exchange_fee`）：

```
record_info 记录：money_remain（现金）、amount_remain（持仓）、price_last、
                  B_continue/S_continue（连续买/卖次数）等
买入：扣 price*amount + fee（含手续费），现金不足则 NO_OPT
卖出：加 price*amount - fee，持仓不足则 NO_OPT
```

回测主循环遍历分钟级历史数据，对每根 K 调用买卖判断（结合昨日 `SAR - close` 的 `sar_diff`），记录每次操作并绘制买卖点散点图（绿=买、红=卖）。

---

## 关键参数速查表

| 参数 | 含义 | 默认值 | 位置 |
|------|------|--------|------|
| `pcr` | 最小操作幅度（过滤微小波动） | 用户配置 / 100 | `config` |
| `quick / slow` | 网格标准差快慢窗口 | 3 / 6 | `StdForReseau/Sub.py` |
| RSV `m` | RSV 均线窗口 | 5 日 | `cal_rsv_class.py` |
| MACD（日/周） | 快/慢/信号 | 6 / 12 / 9 | `select_class.py` |
| MACD（标准/月） | 快/慢/信号 | 12/26/9，月 4/8/9 | `Data_Sub.py` / `Sub.py` |
| SAR | acceleration / maximum | 0.05 / 0.2 | 多处 |
| RSI | timeperiod | 5 / 12 / 30 | `Index` |
| `N_STEPS` | LSTM 时间步 | 20 | LSTM Config |
| 盯盘间隔 | 遍历持仓周期 | 30 秒 | 使用说明书 |

---

## 设计小结

1. **核心是动态网格**：`网格大小 = f(波动率标准差)`，再用 `RSV` 做高低位非对称偏置，是整个软件最有特色的策略。
2. **海选是规则引擎**：所有选股逻辑都抽象成可配置、可按优先级组合的过滤器，扩展性好。
3. **指标库统一**：MACD/RSI/SAR/KD/BOLL/MOM 等全部基于 TA-Lib，封装在 `DataSource/Data_Sub.py::Index`。
4. **强纪律导向**：`has_flashed_flag` 防重复提示、`pcr` 最小操作幅度，都是为了"靠纪律盈利、减少情绪化操作"的产品定位服务。
5. **技术债**：LSTM 依赖 TF 1.x，路径硬编码 Windows 反斜杠，部分回测引用的 `judge_single_stk_sub` 已不在仓库中（历史遗留）。
